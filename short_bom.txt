function demoInts() int myInt, index;{ [*Test File that fulfills <10 lines requirement*]
    get(myInt);
    index = 0;
    while(index <= 9) {
        put(myInt + index); 
        index = index + 1;
    } 
    endwhile
}##return;