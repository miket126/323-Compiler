[*Test File that fulfills <21 lines (and >10 lines) requirement*]
[*Function converts input real to int by truncation assuming > is somehow defined between ints and reals, albeit inefficiently, and returns the int*]
function real2int (myReal real)
    int myInt;
{
    myInt = 0;
    while(myInt < myReal)
    {
        myInt = myInt + 1;
    }
    return myInt - 1;
}

#
real testReal; int testInt;
#

get(testReal);
testInt = real2int(testReal);
put(testInt);