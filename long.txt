[*Test File that fulfills >21 lines requirement*]

[*Function displays the input integer and the 9 ints following*]
function demoInts()
    int myInt, index;
{ 
    get(myInt);
    index = 0;
    while(index <= 9) 
    {
        put(myInt + index); 
        index = index + 1;
    } 
    endwhile
}

[*Function displays the input real, .1 + the input real, .2 + the input real, and so on until 10 reals are shown*]
function demoReals()
    real myReal; int index;
{
    get(myReal);
    index = 0;
    while(index<= 9) 
    {
        put(myReal + index * .1);
        index = index + 1;
    } 
    endwhile
}

[*Function converts input real to int by truncation assuming >, <, and = is somehow defined between ints and reals, albeit inefficiently*]
function real2int (myReal real)
    int myInt;
{
    myInt = 0;
    if(myReal > 0.0)
    {
        while(myInt < myReal)
        {
            myInt = myInt + 1;
        }
        return myInt - 1;
    }
    else
    {
        if(myReal < 0.0)
        {
            while(myInt > myReal)
            {
                myInt = myInt - 1;
            }
            return myInt + 1;
        }
        fi
        return myInt; 
    }
    fi
}
#
real testReal;
#
testReal = 15.1;
put(real2int(testReal));
testReal = testReal - 20.0;
put(real2int(testReal));
